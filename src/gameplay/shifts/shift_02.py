"""
src/gameplay/shifts/shift_02.py

Shift 2 scenario definition — placeholder. Its voltage/AVR and topology/
N-1 tutorial content was merged into shift_01.py (see that file's
docstring) so the whole Phase 2 tutorial arc plays as one continuous
shift. No constants defined; load_shift_config() falls back to its
defaults (empty schedule, AGC off, zero peak demand) if this shift is
loaded.
"""

from __future__ import annotations
